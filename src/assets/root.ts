import { FlexAlignType, ViewStyle } from "react-native";

export default {
    container: {
        flex: 1,
    },
    justifyCenter: {
        justifyContent: 'center',
    } as ViewStyle,
    alignCenter: {
        alignItems: 'center'
    } as ViewStyle,
    verticalMiddle: {
        verticalAlign: 'middle',
    } as ViewStyle,
};
